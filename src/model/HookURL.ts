export class HookURL {

    host: string;
    port: string;
    stepId: string;

    constructor(host: string, port: string, stepId: string) {
        this.host = host;
        this.port = port;
        this.stepId = stepId;

    }

}